[project]
name = "reqtrace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27.0",
  "typer>=0.12.0"
]

[project.scripts]
reqtrace = "reqtrace.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
